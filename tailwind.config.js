const { colors } = require("tailwindcss/defaultTheme")


module.exports = {
  purge: [],
  theme: {
    colors: {
      primary: colors.blue,
      secondary: colors.green,
      neutral: colors.gray,
    },
  },
  variants: {},
  plugins: [],
}
